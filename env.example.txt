# ============================================================
# mcp-server-tasktracker — Environment Configuration Reference
# ============================================================
# Copy this file to .env and fill in the values for your environment.
# Used by docker-compose.yml and compose.traefik.yml.
# ============================================================

# ------------------------------------------------------------
# Task-Tracker connection
# ------------------------------------------------------------

# Base URL of the Task-Tracker instance this server talks to.
# If Task-Tracker runs on the same host (outside this compose network),
# use http://host.docker.internal:8000 instead of localhost.
TASKTRACKER_API_URL=http://your-tasktracker-host:8000

# Note: there is no server-side API token here by design. Each MCP client
# supplies its own Task-Tracker token per request via the Authorization
# header — see README.md's Configuration section.

# ------------------------------------------------------------
# Server
# ------------------------------------------------------------

# Port the server listens on inside the container. Only change this if you
# also update the port mapping/labels in docker-compose.yml or compose.traefik.yml.
PORT=3000

# Set to "true" for verbose logging.
DEBUG=false

# ------------------------------------------------------------
# Traefik only (compose.traefik.yml)
# ------------------------------------------------------------

# Public hostname to route to this server, e.g. mcp-tasktracker.example.com
MCP_DOMAIN=
